{% extends 'base.html.twig' %}

{% block title %}New Comments{% endblock %}

{% block body %}
    
    <div class="row">
         <h1>{{ post.name }}</h1>
        <div class="col-sm col-lg-6 mx-auto mt-5">
            <a href="{{ path('posts_index') }}" class='btn btn-primary d-flex justify-content-lg-start'>back to list</a>
            <div class="card">
                <div class="card-body">
                    {{ include('comments/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
    

    
{% endblock %}
